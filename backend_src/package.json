{
  "name": "backend_src",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "http_api",
      "mongo"
    ]
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "./start.sh",
    "heroku-postbuild": "node ./http_api/app.js &"
  }
}